#pragma once
#include <SFML/Graphics.hpp>

namespace UIColors
{
    inline const sf::Color Background(30, 30, 30);
    inline const sf::Color Box(40, 40, 40);
    inline const sf::Color Border = sf::Color::White;
    inline const sf::Color Active(255, 200, 0);
    inline const sf::Color Text = sf::Color::White;
    inline const sf::Color SliderKnob = sf::Color::Red;
}
